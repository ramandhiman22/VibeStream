<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        /* General Reset */
        body,
        h1,
        h2,
        p,
        ul {
            margin: 0;
            padding: 0;
            list-style: none;
            font-family: 'Arial', sans-serif;
        }

        /* Body Styling */
        body {
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                url("/images/musbackround1_08.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            color: #f5f5f5;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        /* Header Styling */
        header {
            /* background: #1a1a2e; */
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.3);
        }

        .logo p {
            margin: 10px;
        }

        .logo img {
            height: 50px;
            width: 50px;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            display: flex;
        }

        nav ul {
            display: flex;
            gap: 15px;
        }

        nav ul li a {
            color: #f5f5f5;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #1DB954;
        }

        /* Main Profile Section */
        .profile {
            background:rgba(0, 0, 0, 0.6);
            border-radius: 10px;
            padding: 30px;
            margin: 20px auto;
            width: 80%;
            max-width: 600px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
        }

        .profile h2 {
            font-size: 22px;
            margin-bottom: 20px;
            border-bottom: 2px solid #1DB954;
            padding-bottom: 10px;
            color: #1DB954;
        }

        .profile-info p {
            margin-bottom: 15px;
            font-size: 18px;
        }

        .profile-info p strong {
            font-weight: 600;
        }

        .profile-actions {
            text-align: center;
            margin-top: 30px;
        }

        .profile-actions .btn {
            background: #1DB954;
            color: #fff;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            border: none;
            transition: background 0.3s;
            margin: 0 5px;
        }

        .profile-actions .btn:hover {
            background: #6cff9f;
            
        }

        .edit-form {
            display: none; /* Hide form initially */
            margin-top: 30px;
        }

        .profile-img {
            text-align: center;
            margin-bottom: 20px;
        }

        .profile-img img {
            border-radius: 100%;
            width: 500px;
            height: 500px;
            object-fit: cover;
            border: 3px solid #1DB954;
        }

        /* Form Styles */
        .edit-form label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }

        .edit-form input[type="text"],
        .edit-form input[type="email"],
        .edit-form input[type="file"] {
            width: 100%;
            padding: 10px;
            color: white;
            background:rgba(0, 0, 0, 0.6);
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-top: 5px;
        }

        .edit-form button {
            background: #1DB954;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            margin-top: 10px;
            cursor: pointer;
        }

        .edit-form button:hover {
            background: #6cff9f;
        }
    </style>
</head>

<body>
    <header>
        <div class="logo">
            <a id="theme-toggle"><img src="/images/music-player.png" alt="" onclick="window.location.href='/'"></a>
            <p>Vibe Stream</p>
        </div>
    </header>
    <div class="profile-img">
        
    </div>
    <main>
        <section class="profile">
            <div class="profile-img">
                <img src="/<%= user.image || '/images/default-profile.png' %>" alt="Profile Image" style="width: 150px; height: 150px;">
            </div>
            <h2>Profile Details</h2>
            <div class="profile-info">
               
                <p><strong>Username :</strong> <%= user.name %></p>
                <p><strong>Email :</strong> <%= user.email %></p>
                <p><strong>Number :</strong> <%= user.number || 'Not provided' %></p>
                <p><strong>Status :</strong> <%= user.status ? 'Active' : 'Inactive' %></p>
                <p><strong>Account :</strong> <%= user.premium ? 'Premium' : 'Simple' %></p>
            </div>
            <div class="profile-actions">
                <button class="btn" onclick="window.location.href='/user/edit-profile'">Edit Profile</button>
                <form id="logoutForm" action="/user/logout" method="POST" style="display: inline;">
                    <button type="submit" class="btn">Logout</button>
                </form>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 Vibe Stream</p>
    </footer>
</body>

</html>
