<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Open+Sans:wght@200;300;400;500;600;700&display=swap");

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Open Sans", sans-serif;
        }

        body {
            min-height: 100vh;
            width: 100%;
            padding: 0 10px;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),
                url("/images/musbackround1_08.jpg");
            background-size: cover;
            background-repeat: no-repeat;
        }

        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .wrapper {
            width: 400px;
            border-radius: 8px;
            padding: 30px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.5);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            background: rgba(0, 0, 0, 0.5);
        }

        form {
            display: flex;
            flex-direction: column;
        }

        h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #fff;
        }

        label {
            color: #fff;
            margin: 10px 0 5px;
            display: block;
        }

        input {
            width: 100%;
            height: 40px;
            margin-bottom: 15px;
            background: transparent;
            border: 2px solid #ccc;
            outline: none;
            font-size: 16px;
            color: #fff;
            padding: 0 10px;
            border-radius: 5px;
        }

        button {
            background: #fff;
            color: #000;
            font-weight: 600;
            border: none;
            padding: 12px 20px;
            cursor: pointer;
            border-radius: 3px;
            font-size: 16px;
            border: 2px solid transparent;
            transition: 0.3s ease;
        }

        button:hover {
            color: #fff;
            border-color: #fff;
            background: rgba(255, 255, 255, 0.15);
        }

        ul {
            list-style: none;
            padding: 0;
            margin-bottom: 15px;
        }

        ul li {
            color: #ff0000;
        }

        .logo a {
            margin: 10px;
        }

        .logo img {
            height: 50px;
            width: 50px;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            display: flex;
        }
    </style>
</head>

<body>
    <header class="header">
        <div class="logo"><a><img src="/images/music-player.png" onclick="window.location.href='/'"></a></div>
    </header>
    <div class="container">
        <div class="wrapper">
            <h1>Forgot Password</h1>

            <% if (errors && errors.length> 0) { %>
                <ul>
                    <% errors.forEach(error=> { %>
                        <li>
                            <%= error %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>

                    <% if (success && success.length> 0) { %>
                        <p>
                            <%= success %>
                        </p>
                        <% } %>

                            <form action="/auth/forgot-password" method="POST">
                                <label for="email">Email:</label>
                                <input type="email" name="email" required>
                                <button type="submit">Send OTP</button>
                            </form>
        </div>
    </div>
</body>

</html>