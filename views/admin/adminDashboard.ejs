<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <style>
        /* General Reset */
        body,
        h1,
        h2,
        p,
        ul {
            margin: 0;
            padding: 0;
            list-style: none;
            font-family: 'Arial', sans-serif;
        }

        /* Body Styling */
        body {
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                url("/images/musbackround1_08.jpg");
            background-size: cover;
            background-repeat: no-repeat;
            color: #f5f5f5;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        /* Header Styling */
        header {
            /* background: #1a1a2e; */
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.3);
        }

        .logo p {
            margin: 10px;
        }

        .logo img {
            height: 50px;
            width: 50px;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            display: flex;
        }

        nav ul {
            display: flex;
            gap: 15px;
        }

        nav ul li a {
            color: #f5f5f5;
            text-decoration: none;
            font-size: 16px;
            transition: color 0.3s;
        }

        nav ul li a:hover {
            color: #1DB954;
        }

        /* Main Admin Dashboard Section */
        .dashboard {
            /* background: #2f3542; */
            border-radius: 10px;
            padding: 30px;
            margin: 20px auto;
            width: 80%;
            max-width: 600px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
        }

        .dashboard h2 {
            font-size: 22px;
            margin-bottom: 20px;
            border-bottom: 2px solid #1DB954;
            padding-bottom: 10px;
            color: #1DB954;
        }

        .profile-info p {
            margin-bottom: 15px;
            font-size: 18px;
        }

        .profile-info p strong {
            font-weight: 600;
        }

        .profile-actions {
            text-align: center;
            margin-top: 30px;
        }

        .profile-actions .btn {
            background: #1DB954;
            color: #fff;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s;
        }

        .profile-actions .btn:hover {
            background: #55c57c;
        }

        /* Footer Styling */
        footer {
            /* background: #1a1a2e; */
            padding: 10px;
            text-align: center;
            color: #f5f5f5;
            font-size: 14px;
            box-shadow: 0px -2px 6px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>

<body>
    <header>
        <div class="logo">
            <a id="theme-toggle"><img src="/images/music-player.png" alt="" onclick="window.location.href='/'"></a>
            <p>Vibe Stream</p>
        </div>
    </header>

    <main>
        <section class="dashboard">
            <h2>Admin</h2>
            <div class="profile-info">
                <p><strong>Name :</strong> <%= admin.name %></p>
            </div>
            <div class="profile-actions">
                <form id="logoutForm" action="/admin/logout" method="post" style="display: none;"></form>
                <a class="btn" onclick="document.getElementById('logoutForm').submit();">Logout</a>
               <a class="btn" onclick="window.location.href='/track/Upload'">Upload</a>
               <a class="btn" onclick="window.location.href='/track/Delete'">Delete Track</a>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 Vibe Stream</p>
    </footer>
</body>

</html>
